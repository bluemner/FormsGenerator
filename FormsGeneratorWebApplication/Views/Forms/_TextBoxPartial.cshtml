@model  FormsGeneratorWebApplication.Models.TextBoxModel

@{
    var index = Model.postion;
}

@*<div class="ui-accordion-header">
    <div class="form-item">
        <h3>
            @Html.HiddenFor(m => m.question, new { Name = "[" + index + "].question" })
            @Html.Label(Model.question + "?")
        </h3>
    </div>
</div>*@


@*<div class="panel panel-default">
    <div class="panel-heading">Questions :</div>*@
    <div class="panel-body">
        <h3>
            @Html.HiddenFor(m => m.question, new { Name = "[" + index + "].question" })
            @*@Html.Display(m => Model.postion) @**@Html.Label(Model.question)
        </h3>
        <h3>
            @Html.TextAreaFor(m => m.value, new { Name = "[" + index + "].value", @class = "form-control Answer-inputWidthHeight" })
        </h3>
    </div>
@*</div>*@

@*<div>
    <div class="form-horizontal">
        <fieldset>
            <div class="row">
                <div class="form-group">
                    <h3>
                        <label for="inputFormName" class="col-lg-2 control-label ">
                            @Html.HiddenFor(m => m.question, new { Name = "[" + index + "].question" })
                            @Html.Label(Model.question)
                        </label>
                    </h3>
                </div>
                <div class="form-group">
                    <label for="inputFormName" class="col-lg-2 control-label"></label>
                        <div class="col-xs-10">
                            @Html.TextAreaFor(m => m.value, new { Name = "[" + index + "].value", @class = "form-control Answer-inputWidthHeight" })
                        </div>
                </div>
            </div>
            @Html.HiddenFor(m => m.postion, new { Name = "[" + index + "].postion" })
        </fieldset>
    </div>
</div>*@


